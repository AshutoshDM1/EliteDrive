import Section from "@/shared/Section/Sectiont";

const Home = () => {
    return (
        <Section className="flex items-center justify-center h-screen">
            <h1 className="text-2xl text-blue-500">EliteDrive</h1>
        </Section>
    );
};

export default Home;